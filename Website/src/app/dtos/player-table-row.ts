import { BasicData } from './basic-data';

export class PlayerTableRow {
    rank: number;
    skater: BasicData;
    team: BasicData;
    season: number;
    teamCount: number;
    seasonCount: number;
    stats: number[];
}
